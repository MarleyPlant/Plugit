const util = require("../app/util");

module.exports = {
  events: {
    ready: {
      main: function(bot, db) {

      }
    },
    message: {
      main: function(bot, db, msg) {
          //Add XP
      }
    }
  }
}
