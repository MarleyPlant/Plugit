var config = {};

config.token = 'token'
config.clientId = 'client-id'
config.clientSecret = 'client-secret'
config.playing = 'Currently-Playing-Game'
config.prefix = 'Command-Prefix'
config.delete_commands = true;
config.debug = true

module.exports = config;
